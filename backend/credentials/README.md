Coloque aqui o arquivo `google_credentials.json` (credencial da conta de
serviço do Google Cloud, usada para Calendar e Sheets).

Este arquivo **nunca** deve ser commitado — já está protegido pelo
`.gitignore` da raiz do projeto.
